# === Core audio ===
sounddevice>=0.4.6        # microphone capture + playback (PortAudio)
numpy>=1.24
soundfile>=0.12          # wav I/O
scipy>=1.10              # resampling

# === VAD ===
# Silero VAD v5 is loaded via torch.hub or the `silero-vad` pip package
silero-vad>=5.1
torch>=2.1               # CPU or CUDA build; see README

# === STT (Speech-To-Text) ===
faster-whisper>=1.0.3    # fast Whisper implementation (CTranslate2)

# === LLM (local, no API key) ===
llama-cpp-python>=0.3.2  # runs GGUF models via llama.cpp

# === TTS (Text-To-Speech, female voice) ===
kokoro>=0.9.2            # lightweight multilingual TTS with female voices
misaki[ja]>=0.9.3        # Kokoro G2P incl. Japanese support

# === Utilities ===
pyyaml>=6.0
rich>=13.0               # pretty console output
